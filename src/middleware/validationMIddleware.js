export default function (req, res, next) {
    const {
        photo,
        first_name,
        last_name,
        email,
        address,
        city,
        state,
        pin_code,
        about,
        push_notification,
    } = req.body;
}
